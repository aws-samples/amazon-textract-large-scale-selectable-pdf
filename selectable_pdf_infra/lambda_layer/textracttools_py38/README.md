# textracttools_layer_p38

Build an AWS Lambda Layer with the textracttools module. To create the layer, 
update the source wheel in `createlayer.sh` (if required), then run:
```bash
./createlayer.sh 3.8
```

